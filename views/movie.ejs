<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ERIVOX - Movies</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Segoe UI', sans-serif; }
    body { background-color: #0b0b0b; color: #ffffff; min-height: 100vh; }
    header { background: #141414; border-bottom: 2px solid #e50914; padding: 15px 40px; display: flex; justify-content: space-between; align-items: center; }
    .logo { font-size: 28px; font-weight: 900; letter-spacing: 2px; color: #ffffff; text-decoration: none; }
    .logo span { color: #e50914; }
    nav a { color: #b3b3b3; text-decoration: none; margin-left: 20px; font-weight: 600; }
    nav a:hover, nav a.active { color: #e50914; }
    .container { max-width: 1200px; margin: 40px auto; padding: 0 20px; }
    .section-title { font-size: 24px; margin-bottom: 20px; border-left: 4px solid #e50914; padding-left: 10px; }
    .media-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 20px; }
    .media-card { background: #181818; border-radius: 8px; overflow: hidden; border: 1px solid #282828; display: flex; flex-direction: column; }
    .poster { width: 100%; height: 300px; object-fit: cover; background: #222; }
    .card-body { padding: 15px; display: flex; flex-direction: column; flex-grow: 1; }
    .card-title { font-size: 16px; font-weight: bold; margin-bottom: 5px; color: #ffffff; }
    .card-genre { font-size: 12px; color: #e50914; margin-bottom: 10px; font-weight: 600; }
    .btn-download { margin-top: auto; background: #e50914; color: white; text-align: center; padding: 10px; text-decoration: none; border-radius: 4px; font-weight: bold; }
    .no-content { color: #888; text-align: center; grid-column: 1 / -1; padding: 40px; }
  </style>
</head>
<body>

  <header>
    <a href="/" class="logo">ERI<span>VOX</span></a>
    <nav>
      <a href="/">Home</a>
      <a href="/movies" class="active">Movies</a>
      <a href="/tvshows">TV Shows</a>
      <a href="/admin">Admin Panel</a>
    </nav>
  </header>

  <div class="container">
    <h2 class="section-title">Movies Collection</h2>
    <div class="media-grid">
      <% if (typeof movies !== 'undefined' && movies.length > 0) { %>
        <% movies.forEach(item => { %>
          <div class="media-card">
            <img class="poster" src="<%= item.posterUrl || 'https://via.placeholder.com/200x300?text=No+Poster' %>" alt="<%= item.title %>">
            <div class="card-body">
              <div class="card-title"><%= item.title %></div>
              <% if (item.genre) { %><div class="card-genre"><%= item.genre %></div><% } %>
              <a href="<%= item.gofileUrl %>" target="_blank" class="btn-download">Download / Watch</a>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="no-content">No movies added yet.</div>
      <% } %>
    </div>
  </div>

</body>
</html>